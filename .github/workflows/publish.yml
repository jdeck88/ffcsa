name: Deploy Staging

on:
  push:
    branches: [ feature/api_with_vue ]

jobs:
  build:

    runs-on: self-hosted
    defaults:
      run:
        working-directory: /home/ffcsa/app/ffcsa
        
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]

    steps:
    - uses: actions/checkout@v2
      with:
        username: 'mohamedyoussef'
        token: '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'
    - run: git pull origin feature/api_with_vue

    # - name: Set up Python ${{ matrix.python-version }}
    #   uses: actions/setup-python@v2
    #   with:
    #     python-version: ${{ matrix.python-version }}

    # Build frontend
    - name: Build frontend
      run: npm run prod

    # Collect static
    - name: Collect Static
      run: docker exec ffcsa_web python manage.py collectstatic --no-input